Is there a test that can verify the dataset quality? What if the testing part is not protected from overfitted model? Let’s say we find an answer to every test by searching in all the training examples and picking one with the minimum distance. Will we get more than 50% accuracy? Then good performance on such datasets doesn’t tell that an algorithm is any good. It just compresses the training data.